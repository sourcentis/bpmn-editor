// Bundled, OFL-1.1 licensed BPMN icon font (see assets/fonts/OFL-1.1.txt).
// Inlined as a base64 data URI at build time (`?inline`) so the editor works
// standalone — on-canvas glyphs and SVG export both have a working default
// with zero host configuration. `fontUrl` stays overridable via options.
import bpmnFontDataUri from '../assets/fonts/bpmn.ttf?inline';

export const BPMN_FONT_DATA_URI: string = bpmnFontDataUri;
export const BPMN_FONT_FAMILY = 'BPMN';
